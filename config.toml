# The URL the site will be built for
base_url = "http://carlosb1.github.io"

title = "Carlos Baez"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

generate_feed = true
minify_html = true

[markdown]
highlight_code = true
highlight_theme = "ayu-dark"

[extra]
toc = true
use_cdn = false
favicon = "/icon/favicon.png"
theme = "toggle" # light, dark, auto, toggle
# mathjax = true
# mathjax_dollar_inline_enable = true

menu = [
    { name = "/posts", url = "/posts", weight = 1 },
    { name = "/projects", url = "/projects", weight = 2 },
    { name = "/about", url = "/about", weight = 3 },
]

socials = [
    { name = "email", url = "carlos.baezruiz@gmail.com", icon = "email" },
    { name = "github", url = "https://github.com/carlosb1/", icon = "github" },
]

custom_css = []

#[extra.analytics]
#enabled = true
#goatcounter_user = "your_user"
#goatcounter_host = "example.com"    # default= goatcounter.com
